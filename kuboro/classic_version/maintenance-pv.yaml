apiVersion: v1
kind: PersistentVolume
metadata:
  name: php-fpm-maintenance-pv
  labels:
    name: php-fpm-maintenance-pv
  namespace: orocommerce
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteMany
    - ReadOnlyMany
  persistentVolumeReclaimPolicy: Retain
  storageClassName: local-storage
  local:
    path: /orocommerce/php-fpm/maintenance
  nodeAffinity:
    required:
      nodeSelectorTerms:
        - matchExpressions:
            - key: kubernetes.io/hostname
              operator: In
              values:
                - minikube