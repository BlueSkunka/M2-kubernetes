apiVersion: v1
kind: Service
metadata:
  name: orocommerce-websocket
  labels:
    app: orocommerce-websocket
  namespace: orocommerce
spec:
  selector:
    app: orocommerce-websocket
  ports:
  - port: 8080
    targetPort: 8080
  type: ClusterIP
