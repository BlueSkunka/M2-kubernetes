apiVersion: v1
kind: PersistentVolume
metadata:
  name: orocommerce-app-maintenance-pv
  labels:
    name: orocommerce-app-maintenance-pv
  namespace: orocommerce
spec:
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteMany
    - ReadOnlyMany
  persistentVolumeReclaimPolicy: Retain
  storageClassName: nfs-csi
  csi:
    driver: nfs.csi.k8s.io
    # volumeHandle format: {nfs-server-address}#{sub-dir-name}#{share-name}
    # make sure this value is unique for every share in the cluster
    volumeHandle: nfs-server.orocommerce.svc.cluster.local#orocommerce#/orocommerce/maintenance
    volumeAttributes:
      server: nfs-server.orocommerce.svc.cluster.local
      share: /orocommerce/maintenance