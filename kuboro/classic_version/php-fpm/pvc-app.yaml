apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: php-fpm-app-pvc
spec:
  accessModes:
    - ReadWriteMany
  volumeMode: Filesystem
  resources:
    requests:
      storage: 10Gi
  storageClassName: local
  selector:
    matchLabels:
      name: php-fpm-app-pv