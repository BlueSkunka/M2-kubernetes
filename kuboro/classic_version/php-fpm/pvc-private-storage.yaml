apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: php-fpm-private-storage-pvc
spec:
  accessModes:
    - ReadWriteOnce
  volumeMode: Filesystem
  resources:
    requests:
      storage: 10Gi
  storageClassName: local