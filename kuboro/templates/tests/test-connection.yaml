apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "kuboro.fullname" . }}-test-connection"
  labels:
    {{- include "kuboro.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "kuboro.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
